import Head from "next/head";
import { useQuery } from "urql";
import { PRODUCT_QUERY } from "../lib/query";
import Products from "../components/Products";
import Hero from "../components/Hero";

export default function Home() {
  const [results] = useQuery({ query: PRODUCT_QUERY });
  const { data, fetching, error } = results;

  if (fetching) return <p>Loading...</p>;
  if (error) return <p>{error.message}</p>;
  const products = data.products.data;

  return (
    <div className="bg-slate-100">
      <Head>
        <title>Deb's Designs</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
        <link
          href="https://fonts.googleapis.com/css2?family=Leckerli+One&family=Noto+Sans&display=swap"
          rel="stylesheet"></link>
      </Head>

      <Hero />
      <h1 className="text-center my-10 text-4xl font-bold">All Products</h1>
      <div
        id="products-gallery"
        className="grid grid-cols-20 gap-5 justify-items-center mt-20">
        {products.map((product) => (
          <Products key={product.attributes.slug} product={product} />
        ))}
      </div>
    </div>
  );
}
